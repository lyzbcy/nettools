# macOS
.DS_Store
.AppleDouble
.LSOverride

# AI 工具会话记录
.zcode/

# 个人配置（含公司内网信息，勿提交）
# config.json 由 app 运行时在 ~/Library/Application Support/ 下创建
# 这里只排除意外出现在项目目录的情况
config.json

# 编译产物（如需发布成品 app，可删除此行或用 git track .app）
*.o
*.log

# Xcode（本项目不用 Xcode 工程，但保险）
xcuserdata/
DerivedData/

# release zip（二进制大文件，不进 git，通过 GitHub Release 分发）
*.zip
